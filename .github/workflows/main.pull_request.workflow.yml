name: main-pull-request

on:
  pull_request:
    branches:
      - main

jobs:
  raised-pull-request:
    runs-on: self-hosted
    steps:
      - name: test
        run: |
          echo "**************************************************"
          echo "                   PULL REQUEST                   "
          echo "**************************************************"
